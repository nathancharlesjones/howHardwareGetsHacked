Which would I like to finish before continuing Part 2?
>> Refactor tooling
	>> project.py
	>> flashing
	>> debug
	>> conftest
>> Run tests on STM32
	>> Fix flash
>> Run tests on TM4C
	>> Fix linker, init code
	>> Test blinky, serial, single test

Which would I like to finish before finishing Part 2?
>> Simulate
>> Update makefiles to match SCons
>> Add logging
>> Create README
>> Fix debugging

[X] TEST-1 Get all tests passing for STM32 @module(tests) @module(stm32)
	[X] TEST-1.1 Add erasing to flash step during deployment depends:TOOL-1
	[X] TEST-1.2 Fix code that checks flash data, so that it expects all 0xFFs on reset in fob.c
	[X] TEST-1.3 Fix code that checks flash data, so that it expects all 0xFFs on reset in testing files
[ ] TEST-2 Get all tests passing for TM4C @module(tests) @module(tm4c)
	[ ] TEST-2.1 Get one test working for TM4C depends:TEST-2.2
	[ ] TEST-2.2 Talk to TM4C over serial depends:TEST-2.3
	[ ] TEST-2.3 Get to blinky
		[ ] TEST-2.3.1 Fix linker script (no STACK_SIZE, set top at estack)
		[ ] TEST-2.3.2 Add init code
[ ] UNIT-1 Add more unit tests @module(tests)
[ ] CONF-1 Simplify conftest.py @module(tests)
[ ] PING-1 Add "ping" command? @module(tests)
[ ] THRD-1 Add support for three deployed devices
[ ] FLASH-1 Fix flash code @module(fob)
	[ ] FOB-1.1 Decide whether to add .flash_data to TM4C or make STM32 look like starter code
[ ] UI-1 Build console and microui UIs
	[ ] UI-1.1 Fix conditional compilation depends:TOOL-1
	[ ] UI-1.2 Build microui
		[ ] UI-1.2.1 Figure out why checkmark closed test window
		[ ] UI-1.2.2 Run test gui inside microui.c
		[ ] UI-1.2.3 Build fob and car guis
	[ ] UI-1.3 Add "simulate" command to project.py
[X] PROJ-1 Refactor project.py @module(tools)
[ ] CARID-1 Ensure build script updates on new car ID ("Building car didn't trigger new car secret, secrets.h if only car id changed") @module(tools)
[X] FLASHT-1 Decide whether to simplify or remove flashing.py @module(tools)
[ ] SECRET-1 Decide whether to keep or remove CAR_SECRET from secret.h
[ ] STRMEM-1 Check that pins, IDs, and passwords are copied, stored, and compared correctly
	[X] STRMEM-1.1 Fix firmware code
	[ ] STRMEM-1.2 Make opaque struct; use mpack so Python can see it?
[ ] MAKE-1 Update makefiles to match scons @module(tools)
[ ] LOG-1 Add logging
	[ ] LOG-1.1 Define macros
	[ ] LOG-1.2 Add commands
		[ ] LOG-1.2.1 Read logs
		[ ] LOG-1.2.2 Clear logs?
		[ ] LOG-1.2.3 Change severity for function
		[ ] LOG-1.2.4 Global disable?
	[ ] LOG-1.3 Decide whether to add logging to file on x86
	[ ] LOG-1.4 Update SCons?
[ ] DBG-1 Get debugging working
	[ ] DBG-1.1 Install arm-none-eabi-gdb
	[ ] DBG-1.2 Refactor debug command in project.py
[ ] RDME-1 Ask Claude/ChatGPT to create README for repo (incl "how to port")
	[X] RDME-1.1 Install Claude CLI
[X] DOCS-1 Ask Claude/ChatGPT to create Mermaid block/class diagram to show how app and hardware (and ui?) interact
[X] DOCS-2 Ask Claude/ChatGPT to create Mermaid block/class diagram to show how exe and tests interact
[X] TEST-3 Wrap if check on x86 programs so default flash_data.bin file is always made fresh for TEST_BUILDs